import java.awt.*;
import java.io.File;

public class PerimeterRunner {
    public double getPerimeter (Shape s) {
        // start with totolPerm = 0;
        double totalPerim = 0;
        //start with prevPt = last point
        Point prevPt = s.getBounds().getLocation();

        // For each point currPt in the shape
        for (Point currentPoint : s. ) {

        }

            //find distance between two points prevPt and CurrenPt
            //update total Perim to be totolPerim + curretDistance
            //Update prevt to be currpt
    }

    public void testPerimeter () {
//        File file = new File();
//        Shape s = new Sha




    }



    public static void main(String[] args) {
        PerimeterRunner pr = new PerimeterRunner();
        pr.testPerimeter();


        System.out.println("Hello world!");
    }
}