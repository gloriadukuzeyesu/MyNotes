public class RockPaperScissor {
   /**
    * @param word a string to be reversed
    * */

    public String reverseAword(String word) {
        int N = word.length();
        int pointOne = 0;
        int pointerTwo = N - 1;
        StringBuilder sb = new StringBuilder(word);

        while ( pointOne < pointerTwo && pointOne < N ) {

        }


    }

    public static void main( String[] args) {

        String [] rps = {"r", "p", "s"};

        int carspeed = 55;
        if( carspeed > 55 ) {
            System.out.println( "you are at the speed limit");
        }

    }
}
